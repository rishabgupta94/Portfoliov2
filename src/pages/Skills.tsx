import { Grid, Typography } from '@mui/material';
import React from 'react';

export const Skills = () => {
	return (
		<Grid
			container
			justifyContent="center"
			alignItems="center"
			spacing={5}
			height={'100vh'}
		>
			<Typography color="textSecondary">Portfolio is SKILLS PAGE</Typography>
		</Grid>
	);
};
